import React from 'react'
import PropTypes from 'prop-types'

const Habits = ({style}) => (
    <div style={style}>
    </div>
)

Habits.propTypes = {
    style: PropTypes.object,
}

export default Habits
